{
  "name": "3D Matching DevContainer",
  "dockerComposeFile": ["../docker-compose.yml"],
  "service": "app-dev",
  "workspaceFolder": "/workspace",
  "remoteUser": "vscode",
  "mounts": [
    {
      "type": "volume",
      "target": "${containerWorkspaceFolder}/.venv"
    },
    {
      "type": "bind",
      "source": "${localEnv:HOME}/.cache/uv",
      "target": "/home/vscode/.cache/uv"
    }
  ],
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.shell.linux": "/bin/bash",
        "editor.renderWhitespace": "all"
      },
      "extensions": [
        "ms-python.python",
        "charliermarsh.ruff",
        "astal-sh.ty"
      ]
    }
  },
  "postAttachCommand": "sudo chown -R vscode:vscode /workspace /home/vscode && uv sync --all-packages"
}
