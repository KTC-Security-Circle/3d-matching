[project]
name = "3d-matching"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[tool.uv.sources]
core = { workspace = true }
cli = { workspace = true }

[tool.uv.workspace]
members = [
    "app/*",
]

[dependency-groups]
dev = [
    "pre-commit>=4.5.1",
    "ruff>=0.14.14",
    "ty>=0.0.37",
]
